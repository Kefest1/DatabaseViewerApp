import React from 'react';

const BrowseMultipleTables = () => {
    return (
        <div style={{padding: '20px', textAlign: 'center', border: '1px solid #ccc', borderRadius: '5px'}}>
            <h1>Hello, World!</h1>
            <p>This is a sample React component.</p>
            <button onClick={() => alert('Button clicked!')}>Click Me</button>
        </div>
    );
};

export default BrowseMultipleTables;

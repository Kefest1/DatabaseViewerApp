server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    username: username
    password: password
    url: jdbc:postgresql://localhost:5432/database
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
